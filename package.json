{
  "name": "toopo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --typecheck",
    "test:experiment": "vitest run --config experiments/error-convention/sites/vitest.sites.config.ts"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
